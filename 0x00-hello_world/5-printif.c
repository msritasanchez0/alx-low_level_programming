#include <stdio.h>

/**
 * main -A programme that uses the printf function
 * return 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
		return (0);
}
